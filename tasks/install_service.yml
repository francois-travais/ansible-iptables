---

- name: Install iptables save script
  shell:
    cmd: transactional-update -n -d --no-selfupdate shell
    stdin: "{{ lookup('template', 'install_service.sh.j2') }}"
  notify: Reboot

# Force reboot now
- meta: flush_handlers
