---

- name: Install iptables save script
  shell:
    cmd: transactional-update shell -n -d --no-self-update
    stdin: "{{ lookup('template', 'install_service.sh.j2') }}"
  notify: Reboot

# Force reboot now
- meta: flush_handlers
